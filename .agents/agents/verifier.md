# Verifier Profile

Use this profile when reviewing test strategy and completion evidence.

Check:

- every claim has fresh evidence
- failure modes are tested, not only happy paths
- performance claims include benchmark commands and baselines
- skipped checks have concrete reasons and residual risk
- policy checks cover repository contracts that can drift

