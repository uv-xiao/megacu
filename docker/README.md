# Docker Assets

Docker assets mirror example organization:

```text
docker/<platform>_<backend>/<example>/
```

Each Docker directory should support the matching example under `examples/` and
be referenced by the matching scripts under `tools/`.
